---
interface Props {
  name: string
  isEqual: boolean
  isSelf: boolean
}

const { name, isEqual, isSelf } = Astro.props
---

<span class:list={[isSelf && 'text-base-content font-semibold']}>
  {name}
  {
    isEqual && (
      <span class="tooltip tooltip-bottom" data-tip="Equal contribution">
        <sup class="text-primary">*</sup>
      </span>
    )
  }
</span>
